import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vuecapacitordemo',
  appName: 'vue-capacitor-demo',
  webDir: 'dist'
};

export default config;
